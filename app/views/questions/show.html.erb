<div class="container">

  <h4><%= @question.content %></h4>
    <%= image_tag @question.photo %>
  <%= simple_form_for(UserAnswer.new) do |f| %>
    <%= f.association :answer, as: :radio_buttons, collection: @question.answers, label_method: :content, value_method:
    :id, legend_tag: false %>
    <%= f.submit 'Next' %>
  <% end %>
  <% if @question.id == 10 %>
    <%= link_to "Submit answers", user_path(current_user.id), data: {turbo_method: :patch}, class: "btn btn-primary" %>
  <% end %>
</div>
